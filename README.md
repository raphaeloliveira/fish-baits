# fish-baits
List of functions for [Fish Shell](https://fishshell.com/)

## Commands

- `g` = `git`
- `gs` = `git status`
- `gfp` = `git fetch -p`
- `gba` = `git branch -a`

##Installation
Copy the `*.fish` files into `~/.config/fish/functions/`.
